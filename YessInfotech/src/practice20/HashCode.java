package practice20;

public class HashCode {
	public static void main(String args[])  //whenever we don't give any string/val to the output it automatically
	                                        //generates the output as the package_name.hashcode/address of the object created 
	{
		HashCode obj=new HashCode();
		System.out.println(obj);
	}
}
